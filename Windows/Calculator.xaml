<Window x:Class="CookieEdit2.Calculator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Calculator" SizeToContent="Height" Width="300" Height="296.227"
        Style="{StaticResource {x:Type Window}}" BorderBrush="Red" Background="Black" Foreground="#FF00FF3A">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="inputTextBox" Grid.Row="0" Text="1+1" FontSize="40" Margin="5"/>
        <Button x:Name="EvalButton" Grid.Row="1" Content="Evaluate" FontSize="30" FontWeight="Bold" Click="EvalButton_Click" Margin="5" IsDefault="True"/>
        <TextBox x:Name="outputTextBox" Grid.Row="2" FontSize="40" Margin="5"/>
        
    </Grid>
</Window>
